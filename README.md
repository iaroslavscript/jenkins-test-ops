# jenkins-testing-ops
Stop testing Jenkins implementation details. JenkinsTestingOps provides dependency injection and test doubles for pipeline operations. Test business logic, not string-matched shell commands. Plus fluent assertions API with semantic labels for clear, robust tests.
